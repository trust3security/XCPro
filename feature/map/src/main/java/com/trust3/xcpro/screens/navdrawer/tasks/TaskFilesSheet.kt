package com.trust3.xcpro.screens.navdrawer.tasks

// Task files sheet split into focused files:
// - TaskFilesSheetContent.kt (entry + bottom bar + handle)
// - TaskFileSection.kt (airspace/waypoint/classes sections and headers)
// - TaskFileRow.kt (Airspace class card)
// - TaskFileUi.kt (error banner, selected file list)
// - TaskFileModels.kt (models/typealiases)
