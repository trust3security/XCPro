package com.trust3.xcpro.map

/**
 * Shared ADS-B UI test tag for the flashing issue badge.
 *
 * Kept in traffic so moved ADS-B overlay debug panels can use a stable tag
 * without depending on map module test-tag files.
 */
internal const val ADSB_ISSUE_FLASH_BADGE_TAG = "adsb_issue_flash_badge"

